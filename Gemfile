source 'https://rubygems.org'

# Build a persistent domain model by mapping database tables to Ruby classes.
#   Strong conventions for associations, validations, aggregations, migrations,
#   and testing come baked-in.
#
gem 'activerecord', '~> 4.2', '>= 4.2.6'

# FXRuby is a library for developing powerful and sophisticated cross-platform
#   graphical user interfaces (GUIs) for your Ruby applications.
#
gem 'fxruby'

# This module allows Ruby programs to interface with the SQLite3 database
#   engine (http://www.sqlite.org). You must have the SQLite engine installed
#   in order to build this module.
#
gem 'sqlite3'

# Rake is a Make-like program implemented in Ruby. Tasks and dependencies are
#   specified in standard Ruby syntax.
#
gem 'rake'

# SimpleCov is a code coverage analysis tool for Ruby.
#
gem 'simplecov', require: false

# RuboCop is a Ruby static code analyzer. Out of the box it will enforce many
#   of the guidelines outlined in the community Ruby Style Guide.
#
gem 'rubocop', require: false

# RSpec is a Behaviour-Driven Development tool for Ruby programmers. BDD is an
#   approach to software development that combines Test-Driven Development,
#   Domain Driven Design, and Acceptance Test-Driven Planning.
#
gem 'rspec'
